# Cryptography

CIGP composes established primitives only: SHA-256, HMAC-SHA-256, HKDF-SHA-256, Ed25519, and domain-separated Merkle hashing. The implementation does not claim protection against a compromised operator key, compromised build host, or incorrect published game definition.
